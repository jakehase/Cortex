# Window 1 -> Window 2 handoff

Ready for window 2 once durability run 1 completes:
- winner config is locked (`cfg_balanced_roomy`)
- final rerun command and globals file are prepared
- validation command script is prepared
- report skeleton already contains baseline/loop/experiment/triage sections
- remaining live tasks are: durability windows 2/3, final rerun, validation, final completion synthesis
