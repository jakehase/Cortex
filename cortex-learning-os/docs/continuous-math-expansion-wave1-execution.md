# Continuous Mathematics Expansion Wave 1 — Execution Contract

## Anchor and target

- Owner direction: `Continue it` after Phase-0 run `phase0-20260809T041854Z` reported an exhausted 264-node frontier and missing exact-source assessment banks.
- Active target: `/root/clawd/worktrees/cortex-learning-os-applied-math-transfer-20260808/cortex-learning-os` on `feat/cortex-learning-os-continuous-math-evidence-20260808`.
- Fidelity: production additive curriculum expansion plus bounded independent evidence collection; not a prototype and not a qualification claim.
- Scope: preserve all 264 concepts and their evidence, add 24 source-grounded frontier concepts, commission acquisition/validity/retention banks with isolated author/reviewer sessions, migrate signed mastery additively, and launch only the bounded Hetzner evidence lane whose gates are green.
- Non-goals: rewriting old evidence, rerunning the stale bank, activating new transfer profiles before evidence, faking elapsed retention, or changing model weights.

## Expansion surface

The exact source and overlap review is `surfaces/continuous-math-expansion-wave1-v1.json`.

Wave shape:

- 24 concepts
- 6 tracks
- 4 concepts per track
- 264 legacy concepts byte-preserved in `curriculum.phd-trajectory-v1.0.0-264.graph.json`
- target graph version `1.1.0`, 288 concepts
- all added concepts mapped as non-required specialization or qualifying frontier work

Tracks:

1. information theory
2. control and sequential decision systems
3. mathematical learning and inference
4. advanced convex optimization
5. algebraic geometry
6. operator and noncommutative algebra

## Bank separation

Three commissioning roots are mandatory and may not share a model session:

- acquisition: 24 new concepts, one acquisition plus two correction items per concept
- validity: disjoint direct and compositional items for the frozen Phase-0 validity cohort and, separately, future commitments for newly acquired concepts
- retention: two disjoint variants for the frozen 19-concept R7 cohort; sealing does not start or satisfy elapsed-time windows

Each accepted item requires:

- a fresh no-tool `gpt-5.6-sol`/`xhigh` author session
- a separate fresh no-tool reviewer session
- independent recomputation of the checker answer
- positive provider-observed usage
- no tool events
- exact graph/rubric/deployment/campaign binding
- distinct Ed25519 author and reviewer authorities

## Execution placement

- Control plane: source freeze, additive migration, signing, replay, state application, notifier, and artifact publication.
- Hetzner: model authoring/review and scored fresh-session candidate work from an exact isolated Git worktree; the shared execution-plane mirror is never checked out away from its existing source.
- No private authority key is copied into a commissioning model context.
- No heavy worker farm runs on CT101.

## One-smoke gate

Run exactly one focused end-to-end readiness smoke over:

1. canonical program validation at 288 concepts;
2. exact preservation of the first 264 concept records;
3. additive migration dry build preserving all old concept evidence and adding exactly 24 unassessed records;
4. validity-purpose schema mechanics;
5. synthetic unsigned-to-signed bank assembly mechanics using fixture records only;
6. Phase-0 audit behavior with a 288-row matrix and a 264-entry operator subset.

No broad test campaign is part of this change.

## Apply order

1. Commit and push exact expansion/tooling source.
2. Synchronize the exact commit to Hetzner and record source proof.
3. Launch detached, bounded role-isolated bank commissioning under a durable supervisor and control-plane notifier.
4. Return and validate content; sign through four separated gates; publish owner-only banks.
5. Apply additive mastery migration only after bank validation is green.
6. Re-run the read-only audit against 288 rows.
7. Launch the first bounded acquisition wave only when all acquisition gates are green.
8. Schedule validity for 24–72 hours after new acquisition; preserve existing-cohort validity separately.
9. Freeze retention releases now but execute R7 only after real elapsed time.

## Stop conditions

Stop the wave on the first of:

- source/tree drift
- source-preservation failure
- invalid program or migration
- missing/invalid provider ledger
- author/reviewer session or authority collapse
- bank validation failure
- remote execution boundary failure
- non-advancing signed state
- configured call/session/wall-time budget exhaustion

A stopped wave must publish a precise blocker. It must not substitute fixtures or relabel readiness as learning.

## Truth boundary

The expansion makes 24 concepts eligible for independently tested acquisition. Commissioned banks prove bank mechanics and provenance only. Signed acquisition, disjoint validity, elapsed retention, and paired utility remain separate evidence layers.
