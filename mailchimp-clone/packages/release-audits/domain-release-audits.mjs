const PROGRAM={id:'release-audits',name:'Release Audits',focus:'Release Audits adds final continuation analysis lanes for release, audits.',themes:["release","audits"],metrics:["release","audits","coverage","quality"],lanes:["scope","execute","verify","report"]};

export function createReleaseAuditsWorkspace(workspaceName='Closeout workspace'){return {...PROGRAM,workspaceName,generatedAt:new Date().toISOString(),scorecards:PROGRAM.metrics.map((metric,index)=>({id:metric,label:metric.replace(/-/g,' '),currentValue:24+(index*3),targetValue:36+(index*5),posture:index%2===0?'healthy':'watch',narrative:'Release Audits tracks '+metric+' for '+workspaceName+'.'})),programs:PROGRAM.lanes.map((lane,index)=>({id:'release-audits-program-'+(index+1),lane,owner:lane+'-owner',status:index===0?'active':index===1?'planned':index===2?'review':'monitoring',narrative:'Release Audits keeps '+lane+' visibility online.'}))};}

export function summarizeReleaseAudits(workspace=createReleaseAuditsWorkspace()){return {id:workspace.id,name:workspace.name,focus:workspace.focus,workspaceName:workspace.workspaceName,metricCount:workspace.scorecards.length,activePrograms:workspace.programs.filter((entry)=>entry.status==='active').length,watchMetrics:workspace.scorecards.filter((entry)=>entry.posture==='watch').map((entry)=>entry.id)};}

export function createReleaseAuditsNarratives(workspace=createReleaseAuditsWorkspace()){return workspace.programs.map((program,index)=>({id:program.id+'-narrative',headline:workspace.name+' '+program.lane+' closeout',summary:'Closeout narrative '+(index+1)+' for '+workspace.workspaceName+'.',dependencies:workspace.scorecards.slice(0,2).map((card)=>card.id)}));}
