const PROGRAM={id:'approval-batches',name:'Approval Batches',focus:'Approval Batches adds a last continuation closeout lane for approval, batches.',themes:["approval","batches"],metrics:["approval","batches","coverage","quality"],lanes:["scope","ship","verify","report"]};

export function createApprovalBatchesWorkspace(workspaceName='Final ladder workspace'){return {...PROGRAM,workspaceName,generatedAt:new Date().toISOString(),scorecards:PROGRAM.metrics.map((metric,index)=>({id:metric,label:metric.replace(/-/g,' '),currentValue:28+(index*3),targetValue:40+(index*5),posture:index%2===0?'healthy':'watch',narrative:'Approval Batches tracks '+metric+' for '+workspaceName+'.'})),programs:PROGRAM.lanes.map((lane,index)=>({id:'approval-batches-program-'+(index+1),lane,owner:lane+'-owner',status:index===0?'active':index===1?'planned':index===2?'review':'monitoring',narrative:'Approval Batches keeps '+lane+' visibility online.'}))};}

export function summarizeApprovalBatches(workspace=createApprovalBatchesWorkspace()){return {id:workspace.id,name:workspace.name,focus:workspace.focus,workspaceName:workspace.workspaceName,metricCount:workspace.scorecards.length,activePrograms:workspace.programs.filter((entry)=>entry.status==='active').length,watchMetrics:workspace.scorecards.filter((entry)=>entry.posture==='watch').map((entry)=>entry.id)};}

export function createApprovalBatchesNarratives(workspace=createApprovalBatchesWorkspace()){return workspace.programs.map((program,index)=>({id:program.id+'-narrative',headline:workspace.name+' '+program.lane+' ladder',summary:'Ladder narrative '+(index+1)+' for '+workspace.workspaceName+'.',dependencies:workspace.scorecards.slice(0,2).map((card)=>card.id)}));}
