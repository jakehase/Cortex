const PROGRAM={id:'sender-reputation',name:'Sender Reputation',focus:'Sender Reputation adds a last continuation closeout lane for sender, reputation.',themes:["sender","reputation"],metrics:["sender","reputation","coverage","quality"],lanes:["scope","ship","verify","report"]};

export function createSenderReputationWorkspace(workspaceName='Final ladder workspace'){return {...PROGRAM,workspaceName,generatedAt:new Date().toISOString(),scorecards:PROGRAM.metrics.map((metric,index)=>({id:metric,label:metric.replace(/-/g,' '),currentValue:28+(index*3),targetValue:40+(index*5),posture:index%2===0?'healthy':'watch',narrative:'Sender Reputation tracks '+metric+' for '+workspaceName+'.'})),programs:PROGRAM.lanes.map((lane,index)=>({id:'sender-reputation-program-'+(index+1),lane,owner:lane+'-owner',status:index===0?'active':index===1?'planned':index===2?'review':'monitoring',narrative:'Sender Reputation keeps '+lane+' visibility online.'}))};}

export function summarizeSenderReputation(workspace=createSenderReputationWorkspace()){return {id:workspace.id,name:workspace.name,focus:workspace.focus,workspaceName:workspace.workspaceName,metricCount:workspace.scorecards.length,activePrograms:workspace.programs.filter((entry)=>entry.status==='active').length,watchMetrics:workspace.scorecards.filter((entry)=>entry.posture==='watch').map((entry)=>entry.id)};}

export function createSenderReputationNarratives(workspace=createSenderReputationWorkspace()){return workspace.programs.map((program,index)=>({id:program.id+'-narrative',headline:workspace.name+' '+program.lane+' ladder',summary:'Ladder narrative '+(index+1)+' for '+workspace.workspaceName+'.',dependencies:workspace.scorecards.slice(0,2).map((card)=>card.id)}));}
