const PROGRAM={id:'channel-health',name:'Channel Health',focus:'Channel Health adds one more continuation lane for channel, health operations.',themes:["channel","health"],metrics:["channel","health","health","coverage"],lanes:["plan","stage","ship","observe"]};

export function createChannelHealthWorkspace(workspaceName='Final continuation workspace'){return {...PROGRAM,workspaceName,generatedAt:new Date().toISOString(),scorecards:PROGRAM.metrics.map((metric,index)=>({id:metric,label:metric.replace(/-/g,' '),currentValue:20+(index*3),targetValue:32+(index*5),posture:index%2===0?'healthy':'watch',narrative:'Channel Health tracks '+metric+' for '+workspaceName+'.'})),programs:PROGRAM.lanes.map((lane,index)=>({id:'channel-health-program-'+(index+1),lane,owner:lane+'-owner',status:index===0?'active':index===1?'planned':index===2?'review':'monitoring',narrative:'Channel Health keeps '+lane+' work visible.'}))};}

export function summarizeChannelHealth(workspace=createChannelHealthWorkspace()){return {id:workspace.id,name:workspace.name,focus:workspace.focus,workspaceName:workspace.workspaceName,metricCount:workspace.scorecards.length,activePrograms:workspace.programs.filter((entry)=>entry.status==='active').length,watchMetrics:workspace.scorecards.filter((entry)=>entry.posture==='watch').map((entry)=>entry.id)};}

export function createChannelHealthNarratives(workspace=createChannelHealthWorkspace()){return workspace.programs.map((program,index)=>({id:program.id+'-narrative',headline:workspace.name+' '+program.lane+' brief',summary:'Final continuation narrative '+(index+1)+' for '+workspace.workspaceName+'.',dependencies:workspace.scorecards.slice(0,2).map((card)=>card.id)}));}
